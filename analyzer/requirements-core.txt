numpy>=1.26
scipy>=1.11
soundfile>=0.12
# contrib-headless (not plain opencv-python-headless): face detection uses
# cv2.FaceDetectorYN, and headless avoids a display dependency on
# servers/CI. Only install one opencv variant at a time; opencv-python,
# opencv-contrib-python, and their -headless counterparts all ship a
# top-level `cv2` package and conflict if mixed.
opencv-contrib-python-headless>=4.9
matplotlib>=3.8
pydantic>=2.6
pytest>=8.0
webvtt-py>=0.4.6
