# Lip-sync/dialogue-scene detection and the learned per-scene estimator.
# Installed separately from requirements-core.txt so the signal-processing
# only path stays fast to set up.
torch>=2.3
torchaudio>=2.3
torchvision>=0.18
scenedetect>=0.6.4
silero-vad>=6.2

# Needed only if you run `bash analyzer/scripts/fetch_syncnet.sh` or use
# `--use-syncnet`: dependencies of the vendored syncnet_python.
python_speech_features>=0.6
tqdm>=4.0

# mediapipe is intentionally not used for face detection. Its Tasks API
# crashes on macOS with a Metal-backed image pipeline error even with a
# CPU delegate requested. Face detection instead uses OpenCV's
# `cv2.FaceDetectorYN` with a small bundled ONNX model, no GPU dependency.
# The opencv-contrib-headless build it needs is declared once in
# requirements-core.txt to avoid installing two conflicting `cv2` packages.
